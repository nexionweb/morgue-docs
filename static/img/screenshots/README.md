# Doc screenshots

Drop screenshots here using the exact filenames below. Each is already
referenced from the matching docs page, so the moment a file lands it shows up.

Guidelines:
- **Retina / 2×** capture, **dark theme** (the docs default to dark).
- PNG. Trim to the relevant UI; no desktop background.

| Filename | Appears on |
| --- | --- |
| `quick-tour-main.png` | Getting Started → Quick tour (full app window) |
| `capturing-modes.png` | Capturing → Capture modes (extension popover) |
| `organizing-folders-tags.png` | Organizing → overview (sidebar + grid) |
| `boards-canvas.png` | Boards → overview (canvas with items + connectors) |
| `palette-extraction.png` | Palettes & fonts → overview (details panel palette/fonts) |
| `settings-ai.png` | Settings → AI features (the AI tab) |
| `account-dashboard.png` | Licensing & devices → Managing devices (customer dashboard) |
